<?php

namespace Fomiash\Psr40000;

/**
 * Should be a secondary example of classes with FooInterface interfaces and BazInterface, if required.
 * If you want to give a single example of a class, then it must have the FooInterface interface,
 * if two, then the first is the class with the FooInterface interface, the second is the BarInterface,
 * third - with BazInterface. The same hierarchy is observed for composition.
 */
interface BazInterface
{

}